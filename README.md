
# customer-engagement

Simple customer engagement demo app.

Files:
- `app.py` — main application entry.

To run locally:

# customer-engagement

Simple customer engagement demo app.

Files:
- `app.py` — main application entry.

To run locally:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt  # if you create one
python app.py
```

Next steps:
- Create a GitHub repository and push this project.
